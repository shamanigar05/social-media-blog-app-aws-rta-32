version: 0.2

phases:
  build:
    commands:
      - mvn package
      - echo build
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo install
  pre-build:
    commands:
      - echo pre_build
  post_build:
    commands:
      - echo post_build

artifacts:
  files:
    - '**/*'
    - target/social-media-blog-app-0.0.1-SNAPSHOT.jar
  discard-paths: yes
