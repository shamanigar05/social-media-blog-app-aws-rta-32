version: 0.2

phases:
  install:
    runtime-versions:
      java: correcto8
    commands:
      - echo install
  pre-build:
    commands:
      - echo re_build
  build:
    commands:
      - mvn package
      - echo build
  post_build:
    commands:
      - echo post_build

artifacts:
  files:
    - '**/*'
    - target/social-media-blog-app-0.0.1-SNAPSHOT.jar
  discard-paths: yes
